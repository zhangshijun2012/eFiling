package com.sinosoft.efiling.hibernate.dao;

import com.sinosoft.efiling.hibernate.entity.DocumentFile;
import com.sinosoft.util.hibernate.dao.EntityDaoSupport;

public class DocumentFileDao extends EntityDaoSupport<DocumentFile> {

	// /**
	// * @param documentId 对应T_F_DOCUMENT的主键 一般指的是投保单号和批单号 大保单是大保单号
	// * @param id 资料类型对应的id
	// * @return 返回一个单证类型下面没有被禁用的资料类型
	// */
	// public DocumentFile getFileById(String documentId, String id) {
	// StringBuffer hql = new StringBuffer();
	// hql.append(" FROM ").append(DocumentFile.class.getName()).append(" d ");
	// hql.append(" WHERE d.document.id=? AND d.fileType.id=? AND d.status<>?");
	// Object [] parameters = {documentId, id, SystemUtils.DOCUMENT_FILE_STATUS_DISABLED};
	// return (DocumentFile) this.uniqueResult(hql.toString(), parameters );
	// }

}
